"use client";
import { useState } from "react";
import { Plus, Trash2 } from "lucide-react";
import type { ValuedPosition } from "@/domain/types";
import { calculateDca, calculateExit, type ExitLevel } from "@/domain/planning";
import { money, percentage, quantity } from "@/lib/formatters";
import { saveExitPlan } from "@/server/strategy-actions";
import { Field, Message } from "./ui";
import { Metric } from "./overview";

export function DcaPlanner({ position, portfolioValue }: { position: ValuedPosition; portfolioValue: string | null }) {
  const [capital, setCapital] = useState("1000"), [price, setPrice] = useState(position.quote?.price ?? ""), [fee, setFee] = useState("0");
  let result: ReturnType<typeof calculateDca> | null = null;
  try { if (position.costBasis !== null) result = calculateDca({ quantity: position.quantity, costBasis: position.costBasis, capital, price, fee, currentPrice: position.quote?.price ?? null, portfolioValue }); } catch { /* Incomplete drafts have no result. */ }
  return <div className="grid gap-6 lg:grid-cols-[1fr_1.3fr]"><section className="panel space-y-5 p-6"><h2 className="text-sm font-medium">დამატებითი შესყიდვა</h2><Field label="დამატებითი კაპიტალი, საკომისიოს ჩათვლით (USD)"><input inputMode="decimal" value={capital} onChange={e => setCapital(e.target.value)} /></Field><Field label="მოსალოდნელი შესყიდვის ფასი (USD)"><input inputMode="decimal" value={price} onChange={e => setPrice(e.target.value)} /></Field><Field label="საკომისიო (USD)"><input inputMode="decimal" value={fee} onChange={e => setFee(e.target.value)} /></Field><p className="text-xs leading-6 text-muted">სიმულაცია ვარაუდობს ახალი კაპიტალის დამატებას. წილი ფასდება აქტივის მიმდინარე ფასით. ტრანზაქცია ავტომატურად არ იქმნება.</p></section><section className="panel p-6"><h2 className="mb-7 text-sm font-medium">შესყიდვის მოსალოდნელი შედეგი</h2>{result ? <><div className="grid grid-cols-2 gap-7"><Metric label="მიმდინარე საშუალო ფასი" value={money(result.currentAverage)} /><Metric label="ახალი საშუალო ფასი" value={money(result.newAverage)} tone="text-brand" /><Metric label="მიმდინარე რაოდენობა" value={quantity(position.quantity)} /><Metric label="დამატებული რაოდენობა" value={quantity(result.addedQuantity)} /><Metric label="ახალი რაოდენობა" value={quantity(result.newQuantity)} /><Metric label="ახალი თვითღირებულება" value={money(result.newBasis)} /><Metric label="მიმდინარე წილი" value={percentage(position.allocation)} /><Metric label="მოსალოდნელი წილი" value={percentage(result.projectedAllocation)} /></div><p className="mt-7 border-t border-line pt-5 text-xs leading-6 text-muted">თუ შესყიდვა რეალურად განახორციელეთ, დაამატეთ ტრანზაქცია ფაქტობრივი რაოდენობით, ფასითა და საკომისიოთი.</p></> : <Message>{position.costBasis === null ? "გამოთვლისთვის საჭიროა ცნობილი თვითღირებულება." : "შეიყვანეთ დადებითი კაპიტალი და ფასი. საკომისიო კაპიტალზე ნაკლები უნდა იყოს."}</Message>}</section></div>;
}
export function ExitPlanner({ position, portfolioId, initial, preview = false }: { position: ValuedPosition; portfolioId: string; initial?: { feePercent: string; levels: ExitLevel[] } | null; preview?: boolean }) {
  const [levels, setLevels] = useState<ExitLevel[]>(initial?.levels ?? [{ price: "", percentage: "25" }]), [feePercent, setFee] = useState(initial?.feePercent ?? "0"), [message, setMessage] = useState(""), [error, setError] = useState(false), [pending, setPending] = useState(false);
  let result: ReturnType<typeof calculateExit> | null = null;
  try { if (position.costBasis !== null) result = calculateExit({ quantity: position.quantity, costBasis: position.costBasis, feePercent, levels }); } catch { /* Validate drafts without inventing outputs. */ }
  function update(i: number, field: keyof ExitLevel, value: string) { setLevels(l => l.map((item, n) => n === i ? { ...item, [field]: value } : item)); setMessage(""); }
  return <div className="space-y-6"><section className="panel p-6"><div className="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-sm font-medium">გაყიდვის ეტაპები</h2><p className="mt-2 text-xs leading-6 text-muted">ყველა წილი ითვლება მიმდინარე {quantity(position.quantity)} {position.asset.symbol}-იდან. ფასები ეტაპობრივად უნდა იზრდებოდეს.</p></div><div className="w-40"><Field label="საკომისიო (%)"><input value={feePercent} inputMode="decimal" onChange={e => { setFee(e.target.value); setMessage(""); }} /></Field></div></div><div className="space-y-4">{levels.map((level, i) => <div key={i} className="grid grid-cols-[40px_1fr_1fr_32px] items-end gap-3"><span className="pb-3 text-xs text-brand">TP{i + 1}</span><Field label="სამიზნე ფასი (USD)"><input value={level.price} inputMode="decimal" onChange={e => update(i, "price", e.target.value)} placeholder="0.00" /></Field><Field label="გასაყიდი წილი (%)"><input value={level.percentage} inputMode="decimal" onChange={e => update(i, "percentage", e.target.value)} /></Field><button className="mb-2 rounded p-2 text-muted hover:text-negative" aria-label={`TP${i + 1}-ის წაშლა`} disabled={levels.length <= 1} onClick={() => { setLevels(l => l.filter((_, n) => n !== i)); setMessage(""); }}><Trash2 size={15} /></button></div>)}</div><button className="button-secondary mt-5" disabled={levels.length >= 12} onClick={() => setLevels(l => [...l, { price: "", percentage: "10" }])}><Plus size={15} />ეტაპის დამატება</button></section>{result ? <><div className="panel grid grid-cols-2 gap-6 p-6 lg:grid-cols-4"><Metric label="მოსალოდნელი წმინდა შემოსავალი" value={money(result.revenue)} /><Metric label="მოსალოდნელი მოგება" value={money(result.profit)} /><Metric label="დარჩენილი პოზიცია" value={`${quantity(result.remainingQuantity)} ${position.asset.symbol}`} /><Metric label="საშუალო წმინდა გასვლის ფასი" value={money(result.weightedExitPrice)} /></div><section className="panel p-6"><h2 className="text-sm font-medium">კაპიტალის ამოღება</h2><p className="mt-3 text-xs leading-7 text-muted">აღსადგენი თვითღირებულება: {money(position.costBasis)}. {result.alreadyRecovered ? "დარჩენილ პოზიციას ნულოვანი თვითღირებულება აქვს." : result.recoveryLevel ? `კაპიტალი სრულად ამოიღება TP${result.recoveryLevel} ეტაპზე, ამ ეტაპის ${quantity(result.recoveryQuantity!)} ${position.asset.symbol}-ის გაყიდვის შემდეგ.` : "მოცემული ეტაპებით საწყისი თვითღირებულება სრულად ვერ ამოიღება."} დარჩება პოზიციის {percentage(result.remainingPercent)}.</p><div className="mt-5 divide-y divide-line">{result.levels.map((l, i) => <div key={i} className="flex flex-wrap justify-between gap-3 py-3 text-xs"><span className="text-brand">TP{i + 1} · {quantity(l.quantity)} {position.asset.symbol}</span><span>შემოსავალი: {money(l.revenue)}</span><span className="text-muted">მოგება: {money(l.profit)}</span></div>)}</div></section></> : <Message>შეიყვანეთ ზრდადი დადებითი ფასები. წილების ჯამი არ უნდა აღემატებოდეს 100%-ს; თვითღირებულება ცნობილი უნდა იყოს.</Message>}{message && <Message error={error}>{message}</Message>}<div className="flex flex-wrap items-center justify-between gap-4"><p className="text-xs text-muted">გეგმა სავაჭრო დავალებას არ ქმნის.</p>{!preview && <button className="button-primary" disabled={!result || pending} onClick={async () => { setPending(true); try { const response = await saveExitPlan({ portfolioId, assetId: position.assetId, feePercent, levels }); setError(!response.ok); setMessage(response.ok ? "გასვლის გეგმა შენახულია." : response.error); } catch { setError(true); setMessage("შენახვა ვერ მოხერხდა."); } finally { setPending(false); } }}>{pending ? "ინახება…" : "გეგმის შენახვა"}</button>}</div></div>;
}
